# Schemas

The initial schema is exploratory and uses JSON Schema Draft 2020-12 expressed as YAML.

Naming convention:

- `*.schema.yaml`: machine-readable schemas;
- released schemas will use semantic versions;
- experimental identifiers use `v0alpha1`.

A schema can validate representation and internal consistency. It cannot independently establish legal, archival or organizational compliance.
